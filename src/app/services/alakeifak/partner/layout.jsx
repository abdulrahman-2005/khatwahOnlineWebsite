export const metadata = {
  title: "شركاء على كيفك | لوحة التحكم",
  manifest: "/api/alakeifak/manifest?slug=partner"
};

export default function PartnerLayout({ children }) {
  return <>{children}</>;
}
